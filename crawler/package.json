{
  "name": "swift-secure-crawler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Verification crawler: checks that a casino's games load from real provider or aggregator infrastructure, and that its licence exists in the regulator's register.",
  "scripts": {
    "scan": "node --experimental-strip-types src/cli.ts scan",
    "licence": "node --experimental-strip-types src/cli.ts licence",
    "learn": "node --experimental-strip-types src/cli.ts learn",
    "footer": "node --experimental-strip-types src/cli.ts footer",
    "install-browser": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
